<!-- Loaded only when com.intellij.mcpServer is present (bundled in IDEs 2025.2+). See the
     optional <depends> in plugin.xml. -->
<idea-plugin>
    <extensions defaultExtensionNs="com.intellij.mcpServer">
        <mcpToolset implementation="pl.archiprogram.localreview.mcp.LocalReviewToolset"/>
    </extensions>
</idea-plugin>
